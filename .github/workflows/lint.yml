# This workflow will validate the dictionary.txt file by checking each line against a regex
name: Lint Dictionary

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Lint dictionary.txt
      run: |
        echo "Validating dictionary.txt"
        INVALID_LINES=$(grep -v -n -P -e '^(\S*?\t.*?\t|# Gboard Dictionary version:1)$' ${GITHUB_WORKSPACE}/dictionary.txt)
        if [ "$INVALID_LINES" != "" ]; then
            echo "ERROR: Invalid lines in dictionary.txt"
            echo "$INVALID_LINES"
            exit 1
        fi
        echo "Dictionary.txt is valid"
        exit 0
